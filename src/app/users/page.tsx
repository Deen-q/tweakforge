
// NextJS relies on convention, not configuration
// i.e., the routing in NextJS, is looking for a page.tsx

// This page displays what component would be seen at /users

const UsersPage = () => {
    return (
        <div>Users Page</div>
    )
}

export default UsersPage